@using Microsoft.AspNetCore.Identity
@using apc.Data

@inject SignInManager<ApplicationUser> SignInManager
@inject NavigationManager NavigationManager
@rendermode InteractiveServer

<DxTabs ActiveTabIndex="ActiveTabIndex"
        ActiveTabIndexChanged="@OnTabIndexChanged">
    @foreach(var tab in tabs) {
        <DxTab TabIconCssClass="@($"tab-icon icon {tab.IconCssClass}")"
        Text="@tab.Text"
        Visible="@tab.Visible" />
    }
</DxTabs>

@code {
    bool hasExternalLogins;
    ManageNavMenuTabModel[] tabs = Array.Empty<ManageNavMenuTabModel>();

    int ActiveTabIndex { get; set; }

    protected override async Task OnInitializedAsync() {
        hasExternalLogins = (await SignInManager.GetExternalAuthenticationSchemesAsync()).Any();

        tabs = [
            new("Perfil", "Account/Manage", "icon-profile"),
            new("Correo", "Account/Manage/Email", "icon-email"),
            new("Contraseña", "Account/Manage/ChangePassword", "icon-password"),
            new("Inicio de sesión externo", "Account/Manage/ExternalLogins", "icon-external") { Visible = hasExternalLogins },
            new("Verificación en dos pasos", "Account/Manage/TwoFactorAuthentication", "icon-two-factor"),
            new("Datos personales", "Account/Manage/PersonalData", "icon-personal")
        ];
    }

    protected override void OnAfterRender(bool firstRender) {
        ActiveTabIndex = Array.FindLastIndex(tabs, tab => NavigationManager.Uri.Contains(tab.Uri));
        if(ActiveTabIndex >= 0)
            StateHasChanged();
    }

    private void OnTabIndexChanged(int index) {
        if(index != ActiveTabIndex) {
            ActiveTabIndex = index;
            NavigationManager.NavigateTo(tabs[index].Uri);
        }
    }

    class ManageNavMenuTabModel(string text, string uri, string iconCssClass) {
        public string Text { get; } = text;
        public string Uri { get; } = uri;
        public string IconCssClass { get; } = iconCssClass;
        public bool Visible { get; init; } = true;
    }
}